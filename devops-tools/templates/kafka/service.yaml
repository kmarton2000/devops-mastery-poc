apiVersion: v1
kind: Service
metadata:
  name: kafka-service
  namespace: {{ include "devops-suite.namespace" . }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.kafka.servicePort }}
      targetPort: 9092
  selector:
    app: kafka