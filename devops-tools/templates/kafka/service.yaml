apiVersion: v1
kind: Service
metadata:
  name: kafka-service
  namespace: devops
spec:
  type: NodePort
  ports:
    - name: internal
      port: 9092
      targetPort: 9092
    - name: external
      port: 9094
      targetPort: 9094
      nodePort: 32400
  selector:
    app: kafka